import { Card, CardContent } from '@mui/material';

import TestUserSelect from './UserForm';
const Search = () => {
  return (
    <Card>
      <CardContent>
        <TestUserSelect />
      </CardContent>
    </Card>
  );
};

export default Search;
